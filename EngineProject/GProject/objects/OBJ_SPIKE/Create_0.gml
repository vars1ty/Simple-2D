/// @description Prevent spawning inside other objects.
// Rotate the object to be facing downwards.
image_angle = -180;
while (place_meeting(x, y, OBJ_SPIKE))
{
	x = random(room_width)
	y = random(5)
}
